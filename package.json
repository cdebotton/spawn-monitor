{
  "name": "spawn-monitor",
  "version": "1.1.3",
  "description": "A tiny little library for spawning up a forever-monitor while outputting color coded and timestamped logs.",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "scripts/build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-runtime": "^5.8.25",
    "chalk": "^1.1.1",
    "forever-monitor": "^1.7.0",
    "moment": "^2.10.6",
    "pad": "^1.0.0"
  },
  "devDependencies": {
    "babel": "^5.8.23"
  }
}
